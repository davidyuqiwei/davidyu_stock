library(quantmod)
dateStart <- "1998-01-01"
dateEnd <- Sys.Date()
index="000001.ss"
    result=getSymbols(index[i],from =dateStart,to = dateEnd,src = "yahoo",auto.assign=FALSE)
    result=getSymbols(index,from =dateStart,to = dateEnd,src = "yahoo",auto.assign=FALSE)
head(result)
tail(result)
    time1=time(result)
time1
clear
